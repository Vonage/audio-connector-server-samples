[project]
name = "mcp-tool-server"
version = "0.1.0"
description = "Mock MCP-style tool server for calendar + tickets."
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn[standard]>=0.27.0",
  "pydantic>=2.0.0",
  "python-dotenv>=1.0.1",
]
